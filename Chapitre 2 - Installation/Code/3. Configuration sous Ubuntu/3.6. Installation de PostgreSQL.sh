# Installation de PostgreSql
sudo apt install postgresql -y

# Changement d'utilisateur
sudo -i -u postgres

# Connexion à la base de données
psql

# Requête SQL
Select 1;

# Sortir du mode requête SQL
\q